name: nonoflex_download
description: nonoFlex download Page

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# NonoFlexAlpha 프로젝트 빌드 버전을 따름
version: 1.0.1+2

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  qr_flutter: ^4.0.0
  http: ^0.13.5
  path_provider: ^2.0.11
  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Icon-nono.png
